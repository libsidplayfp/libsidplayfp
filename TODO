* move mixer into resid specific class
* split sid loaders in a hierarchy
* remove embedded roms
* share resampling code between resid emulations

* finish documenting API
* fix Visual Studio build files (or purge them)

* merge builders into main lib (?)
* add a STIL parser (?)
* make the VIC II emulation cycle exact (?)
